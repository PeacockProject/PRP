#!/bin/sh
set -eu

LIB=/usr/lib/prp/subparts-mount.sh
if [ ! -r "$LIB" ]; then
  printf '%s\n' "mount-peacock: missing library $LIB"
  exit 1
fi

# shellcheck source=/usr/lib/prp/subparts-mount.sh
. "$LIB"

mount_subparts "${1:-}"
